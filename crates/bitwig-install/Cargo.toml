[package]
name = "bitwig-install"
description = "Locating a Bitwig Studio installation and the user data that belongs to it"
version.workspace = true
edition.workspace = true
license = "MIT OR Apache-2.0"
repository.workspace = true

[dependencies]
thiserror.workspace = true
sysinfo.workspace = true

# Asking whether an installation can be written means building one that cannot.
[dev-dependencies]
tempfile.workspace = true
